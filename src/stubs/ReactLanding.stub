import React from 'react';
import { Head, Link } from '@inertiajs/react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { ArrowRight, Zap, Shield, Layers, Code2, Boxes, Sparkles } from 'lucide-react';

export default function Landing() {
    const features = [
        {
            icon: Layers,
            title: 'Domain Isolation',
            description: 'Keep your business logic perfectly organized with zero-config domain isolation that scales with your team.',
        },
        {
            icon: Zap,
            title: 'Action Factories',
            description: 'Reusable, single-responsibility actions that replace bloated controllers and keep your codebase clean.',
        },
        {
            icon: Shield,
            title: 'Type-Safe DTOs',
            description: 'Data transfer objects with full TypeScript support, ensuring type safety across your entire stack.',
        },
        {
            icon: Boxes,
            title: 'Repository Pattern',
            description: 'Eloquent repositories with built-in caching, query filters, and soft delete support out of the box.',
        },
        {
            icon: Code2,
            title: 'Full-Stack Harmony',
            description: 'Inertia.js integration that makes PHP and React/Vue feel like a single, cohesive language.',
        },
        {
            icon: Sparkles,
            title: 'Modern UI Ready',
            description: 'Pre-configured with shadcn/ui components for beautiful, accessible interfaces from day one.',
        },
    ];

    return (
        <div className="min-h-screen bg-background">
            <Head title="Welcome" />

            {/* Header */}
            <header className="sticky top-0 z-50 w-full border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
                <div className="container flex h-16 items-center justify-between">
                    <div className="flex items-center gap-2">
                        <div className="h-8 w-8 rounded-lg bg-primary flex items-center justify-center">
                            <Layers className="h-4 w-4 text-primary-foreground" />
                        </div>
                        <span className="font-bold text-xl">Domion</span>
                    </div>

                    <nav className="flex items-center gap-4">
                        <Link href={route('login')}>
                            <Button variant="ghost">Sign in</Button>
                        </Link>
                        <Link href={route('register')}>
                            <Button>Get Started</Button>
                        </Link>
                    </nav>
                </div>
            </header>

            {/* Hero Section */}
            <section className="container py-24 md:py-32">
                <div className="flex flex-col items-center text-center gap-8">
                    <Badge variant="secondary" className="px-4 py-1.5">
                        <Sparkles className="mr-2 h-3.5 w-3.5" />
                        Built for Laravel 11+
                    </Badge>

                    <h1 className="text-4xl md:text-6xl lg:text-7xl font-bold tracking-tighter max-w-4xl">
                        Domain-Driven Design for{' '}
                        <span className="text-primary">Modern Laravel</span>
                    </h1>

                    <p className="text-xl text-muted-foreground max-w-2xl">
                        The enterprise framework that transforms your Laravel application into a clean,
                        scalable architecture. Build faster, maintain easier, ship confidently.
                    </p>

                    <div className="flex flex-col sm:flex-row gap-4">
                        <Link href={route('register')}>
                            <Button size="lg" className="gap-2">
                                Start Building
                                <ArrowRight className="h-4 w-4" />
                            </Button>
                        </Link>
                        <a href="https://github.com/samushi/domion" target="_blank" rel="noopener noreferrer">
                            <Button size="lg" variant="outline">
                                View on GitHub
                            </Button>
                        </a>
                    </div>

                    {/* Code Preview */}
                    <div className="mt-12 w-full max-w-3xl">
                        <Card className="bg-zinc-950 border-zinc-800">
                            <CardContent className="p-6">
                                <pre className="text-sm text-zinc-300 overflow-x-auto">
                                    <code>{`# Install Domion in your Laravel project
composer require samushi/domion

# Run the setup wizard
php artisan domion:setup

# Create your first domain
php artisan domion:domain Product`}</code>
                                </pre>
                            </CardContent>
                        </Card>
                    </div>
                </div>
            </section>

            {/* Features Section */}
            <section className="container py-24 border-t">
                <div className="text-center mb-16">
                    <h2 className="text-3xl md:text-4xl font-bold tracking-tight mb-4">
                        Everything you need to build at scale
                    </h2>
                    <p className="text-lg text-muted-foreground max-w-2xl mx-auto">
                        Domion provides the architectural foundation that growing teams need,
                        without the complexity of traditional DDD frameworks.
                    </p>
                </div>

                <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                    {features.map((feature, index) => (
                        <Card key={index} className="group hover:shadow-lg transition-shadow">
                            <CardHeader>
                                <div className="h-12 w-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4 group-hover:bg-primary/20 transition-colors">
                                    <feature.icon className="h-6 w-6 text-primary" />
                                </div>
                                <CardTitle>{feature.title}</CardTitle>
                                <CardDescription className="text-base">{feature.description}</CardDescription>
                            </CardHeader>
                        </Card>
                    ))}
                </div>
            </section>

            {/* CTA Section */}
            <section className="container py-24 border-t">
                <Card className="bg-primary text-primary-foreground">
                    <CardContent className="flex flex-col md:flex-row items-center justify-between gap-8 p-8 md:p-12">
                        <div>
                            <h3 className="text-2xl md:text-3xl font-bold mb-2">Ready to transform your architecture?</h3>
                            <p className="text-primary-foreground/80">
                                Join thousands of developers building better Laravel applications with Domion.
                            </p>
                        </div>
                        <Link href={route('register')}>
                            <Button size="lg" variant="secondary" className="gap-2 whitespace-nowrap">
                                Get Started Free
                                <ArrowRight className="h-4 w-4" />
                            </Button>
                        </Link>
                    </CardContent>
                </Card>
            </section>

            {/* Footer */}
            <footer className="border-t py-12">
                <div className="container flex flex-col md:flex-row items-center justify-between gap-4">
                    <div className="flex items-center gap-2">
                        <div className="h-6 w-6 rounded bg-primary flex items-center justify-center">
                            <Layers className="h-3 w-3 text-primary-foreground" />
                        </div>
                        <span className="font-semibold">Domion</span>
                    </div>
                    <p className="text-sm text-muted-foreground">
                        Built with Laravel & React. Open source under MIT License.
                    </p>
                </div>
            </footer>
        </div>
    );
}
