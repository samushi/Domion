import { cn } from '@/lib/utils';

interface AppContentProps {
    variant?: 'header' | 'sidebar';
    children: React.ReactNode;
}

export function AppContent({ variant = 'header', children }: AppContentProps) {
    return (
        <main className={cn('mx-auto flex h-full w-full max-w-7xl flex-1 flex-col gap-4 p-4')}>
            {children}
        </main>
    );
}
