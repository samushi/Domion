<div class="min-h-screen flex items-center justify-center bg-zinc-50 dark:bg-zinc-900 p-4">
    <div class="w-full max-w-md">
        <flux:card class="p-8">
            <div class="text-center mb-8">
                <div class="flex justify-center mb-4">
                    <div class="h-12 w-12 rounded-xl bg-primary flex items-center justify-center">
                        <flux:icon.key class="size-6 text-white" />
                    </div>
                </div>
                <flux:heading size="xl">Forgot password?</flux:heading>
                <flux:subheading>No worries, we'll send you reset instructions</flux:subheading>
            </div>

            @if (session('status'))
                <flux:callout variant="success" class="mb-6">
                    {{ session('status') }}
                </flux:callout>
            @endif

            <form wire:submit="sendResetLink" class="space-y-6">
                <flux:input
                    wire:model="email"
                    label="Email"
                    type="email"
                    placeholder="name@example.com"
                    autofocus
                />

                <flux:button type="submit" variant="primary" class="w-full">
                    <flux:icon.paper-airplane class="size-4 mr-2" wire:loading.remove />
                    <flux:icon.loading class="size-4 mr-2" wire:loading />
                    Send reset link
                </flux:button>
            </form>

            <div class="mt-6 text-center">
                <flux:link href="{{ route('login') }}" class="inline-flex items-center text-sm">
                    <flux:icon.arrow-left class="size-4 mr-2" />
                    Back to login
                </flux:link>
            </div>
        </flux:card>
    </div>
</div>
